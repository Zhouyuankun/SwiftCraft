# SwiftCraft 地形生成计划

> 状态：地形内容与后续性能优化均已完成验收
> 本文记录分节点实施方案与验收状态。

## 1. 目标

将当前所有区块完全相同的 8 层平坦世界，改造成一个满足以下条件的程序化地形系统：

- 相同种子始终生成相同世界。
- 地形在 Chunk 边界连续，不出现接缝或断层。
- 首版提供平原、丘陵、低地/沙地，以及基础矿物分布。
- 玩家挖掘后的修改仍优先于生成结果，并在本次运行期间保留。
- 地形生成与 Metal 网格生成分离，后续可独立加入洞穴、树木、生物群系和存档。
- 生成过程不阻塞每帧渲染；首版先保证结构正确，再进行异步化和性能优化。

## 2. 当前约束与问题

当前实现位于 `SwiftCraft/Blocks.swift`：

- `Chunk` 尺寸为 `5 × 8 × 5`，世界总高度只有 8 格，无法表现有层次的地形。
- `Chunk.init()` 同时承担数据初始化与固定地形生成，无法传入世界坐标或种子。
- `World.update(center:)` 在新区块出现时直接调用 `Chunk()`，缺少独立生成器。
- 世界只有水平 Chunk，没有垂直 Chunk；首版可继续保留这一模型，但需要提高固定世界高度。
- `chunkData` 永不淘汰，探索距离增加后内存会持续增长。
- 相邻 Chunk 未生成时，网格构建把边界视为空气；相邻数据出现后依靠重建消除多余面。
- 当前没有测试 Target，负坐标、边界连续性和种子稳定性只能靠人工验证。

## 3. 首版范围（MVP）

首版建议只做“可稳定游玩的地表地形”，暂不加入洞穴、树木、水体模拟和复杂生物群系。

### 3.1 世界参数

建议将参数集中到一个配置结构中，不再散落为魔法数字：

| 参数 | 建议初值 | 说明 |
| --- | ---: | --- |
| Chunk 宽/深 | `16 × 16` | 减少区块切换和小网格数量；若希望先降低改动风险，可暂保留 `5 × 5` |
| 世界高度 | `64` | 保留单层垂直 Chunk 模型，同时提供足够地形空间 |
| 海平面 | `24` | 用于低地沙层和后续水体 |
| 基准地面 | `28` | 大部分平原高度中心 |
| 最大地表高度 | `56` | 给天空和后续树木预留空间 |
| 世界种子 | 每次启动随机生成 | 单次运行内固定，并显示/记录以便复现问题 |

Chunk 尺寸已确认为 `16 × 64 × 16`；生成算法本身不能依赖具体 Chunk 尺寸。

### 3.2 地形组成

每个世界坐标柱 `(worldX, worldZ)` 独立计算地表高度，填充规则为：

1. `y = 0`：基岩。
2. 地表以下较深区域：石头。
3. 地表以下 3～4 格：泥土。
4. 普通地表：草方块。
5. 海平面附近的低地：顶部及浅层改为沙子。
6. 石层中按确定性三维噪声少量替换为煤矿。
7. 地表以上：空气。

首版不生成水，海平面只作为地貌和材质分界。这样无需立即引入透明材质、液体网格和渲染排序。

## 4. 生成算法

### 4.1 确定性随机源

- 所有结果只由 `worldSeed + 世界坐标 + 生成通道盐值` 决定。
- 禁止使用调用顺序相关的全局随机数，否则区块加载顺序会改变世界内容。
- 高度、矿物、未来的洞穴和植被使用不同盐值，避免调整一个系统扰动其他系统。
- 坐标哈希必须明确使用固定宽度整数与溢出运算，确保同一版本中结果稳定。

### 4.2 高度图

使用多尺度二维噪声组合，而不是对每个 Chunk 单独随机：

```text
continental = lowFrequencyNoise(worldX, worldZ)
detail      = mediumFrequencyNoise(worldX, worldZ)
ridge       = optionalRidgeNoise(worldX, worldZ)

height = baseHeight
       + continental * broadAmplitude
       + detail * detailAmplitude
       + ridge * ridgeAmplitude
```

关键原则：噪声输入必须使用绝对世界坐标，不能使用 Chunk 局部坐标，因此边界两侧天然连续。

实现选择建议：

- MVP 使用项目内自有的 seeded value noise/fBm，避免增加第三方依赖。
- 插值使用平滑曲线，至少保证高度变化连续。
- 统一将最终高度 clamp 到合法范围，并为顶部预留安全空间。
- 所有频率、振幅和 octave 数放入配置，以便快速调参。

### 4.3 地貌分区

首版不建立完整生物群系对象，而是根据低频噪声和高度得到连续地貌：

- 低值：低地，靠近海平面时使用沙子。
- 中值：平原，以低振幅细节为主。
- 高值：丘陵，叠加脊状或更高振幅噪声。

分区边界应使用平滑混合，避免按阈值直接切换振幅造成明显台阶。正式生物群系留到第二阶段，通过温度/湿度图扩展。

### 4.4 矿物

- 仅在石头中生成煤矿。
- 使用三维坐标哈希或低成本三维噪声，结果与区块加载顺序无关。
- 概率可随深度变化，但首版保持简单且可配置。
- 矿物判断晚于基础层填充，早于玩家修改覆盖。

## 5. 代码结构调整

计划新增或重构以下职责，具体文件名可在实现时微调：

```text
TerrainConfiguration
  └─ 世界尺寸、种子、海平面、噪声参数、层厚与矿率

SeededNoise / CoordinateHash
  └─ 无状态、确定性的二维/三维采样

TerrainGenerator
  ├─ surfaceHeight(worldX:worldZ:)
  ├─ blockType(worldX:y:worldZ:)
  └─ generateChunk(at:)

World
  ├─ 请求 TerrainGenerator 创建缺失数据
  ├─ 保存已生成 Chunk 与玩家修改
  └─ 负责加载集合和网格重建

GeometryFactory
  └─ 只读取方块数据并构建网格，不参与地形规则
```

具体改造点：

1. 将固定地形逻辑移出 `Chunk.init()`；`Chunk` 只负责分配/承载方块数据。
2. `World` 持有一个带 seed/config 的 `TerrainGenerator`。
3. `World.update(center:)` 发现缺失区块时调用 `generateChunk(at:)`。
4. 所有生成采样使用 `ChunkCoord` 转换后的绝对世界坐标。
5. 将世界坐标到 Chunk/局部坐标的换算收敛为公共函数，避免查询、破坏、生成各自重复实现。
6. 保持当前 `chunkData` 优先：已生成或已修改区块不得因重新进入加载范围而再次覆盖。

## 6. 数据与性能计划

### 6.1 数据布局

当前 `[[[BlockType]]]` 会产生大量小数组和边界检查。实施可分两步：

- MVP：保持现有布局，先验证生成正确性。
- 优化阶段：改为一维连续数组，索引为 `(y * depth + z) * width + x`，减少分配并方便序列化。

如果直接采用 `16 × 64 × 16`，建议在同一轮改成一维存储，否则嵌套数组的对象数量和访问开销会明显增加。

### 6.2 加载与卡顿

首版先同步生成，但需要记录单个 Chunk 的“数据生成耗时”和“网格生成耗时”。当加载半径或 Chunk 尺寸提升后，再加入：

- 后台队列生成纯 CPU 方块数据和顶点数组。
- 主线程/渲染线程仅创建或替换 `MTLBuffer`。
- 每帧限制新区块提交数量，避免跨边界时集中卡顿。
- 为区块增加状态：缺失、生成中、待上传、已加载。
- 使用 generation token 防止过期异步任务把已经离开范围的 Chunk 重新上传。

异步化不能早于确定性和边界测试，否则并发问题会掩盖生成问题。

### 6.3 内存

MVP 可暂时延续“所有访问过的数据常驻内存”，但应记录为明确限制。后续采用：

- 未修改区块可直接淘汰，回来时用种子重新生成。
- 修改过的区块只保存差异集合，或写入磁盘后淘汰。
- GPU 网格继续只保留渲染距离内区块。

## 7. 测试与验收

建议增加轻量 XCTest Target；生成器应不依赖 Metal，从而可以纯 CPU 测试。

### 7.1 自动测试

- 同一种子、同一坐标重复生成，方块数组完全一致。
- 不同种子在抽样区域产生不同高度图。
- 以不同加载顺序生成同一批 Chunk，结果完全一致。
- 正坐标、零点和负坐标的世界/Chunk/局部坐标换算正确。
- 相邻 Chunk 边界柱的高度等于直接按对应世界坐标采样的高度。
- 所有地表高度均在配置合法范围内。
- 每根柱子只有空气位于地表以上，地表以下没有意外空洞（洞穴功能加入前）。
- 基岩、土层、草地/沙地和煤矿的生成约束正确。
- 玩家移除方块后，重新加载该 Chunk 不会被生成器恢复。

### 7.2 人工验收

- 从原点连续飞越至少 10 个 Chunk，无边界裂缝和突变直墙。
- 穿越负 X、负 Z 及两个负轴交界区域，地形连续。
- 能肉眼识别平原、缓坡、丘陵和低地，但整体仍适合当前自由相机浏览。
- 快速跨越 Chunk 边界时无明显长时间停顿。
- 挖掘边界方块后，两侧 Chunk 网格都正确刷新。
- 记录某次启动的种子，并用该种子重新生成时，未修改的世界外观一致。

## 8. 分节点实施与人工验收

采用严格的逐节点开发方式：**一次只实施一个节点；完成构建检查和本节点验证后停止，等待人工确认。未经确认不进入下一节点。** 如果某个节点验证失败，只修复该节点，不顺带实现后续功能。

### 节点 0：建立当前版本基线

改动范围：不改变业务行为，只记录构建结果和当前人工检查基线。

开发侧检查：

- 工程能够成功构建。
- 记录启动位置、平坦地形、区块加载和挖掘行为。

人工验证：启动程序，确认现有平坦世界可移动、可跨 Chunk、可挖方块。

### 节点 1：Chunk 数据结构与尺寸升级

状态：已通过用户验收。

改动范围：

- 将 Chunk 改为已确认的 `16 × 64 × 16`。
- 方块存储改为一维连续数组，并集中索引访问。
- 暂时仍生成等价的平坦分层地形，不引入噪声。
- 调整初始相机高度，避免出生在方块内部。

开发侧检查：索引边界、负坐标查询、区块边界挖掘以及构建检查。

人工验证：世界仍然是平地；移动和挖掘正常；跨越更大的 Chunk 边界时没有裂缝或崩溃。

### 节点 2：生成器解耦（仍保持平地）

状态：已通过用户验收。

改动范围：

- `Chunk` 只作为方块数据容器。
- 新增配置和 `TerrainGenerator`，先实现当前平坦规则。
- `World` 通过生成器创建缺失 Chunk。
- 统一世界坐标、Chunk 坐标和局部坐标换算。

开发侧检查：新旧平坦分层结果一致；正负坐标换算正确；工程成功构建。

人工验证：画面和节点 1 基本一致；跨正负坐标轴、加载新区块、挖掘并返回原区域均正常。

### 节点 3：随机种子与二维噪声基础

状态：已通过用户验收。

改动范围：

- 每次应用启动生成一个随机世界种子，该次运行内固定不变。
- 将本次种子输出到调试信息（建议 HUD 与控制台至少保留一处），便于报告问题和复现。
- 实现无状态坐标哈希、二维 value noise 和 fBm。
- 本节点只接入测试/诊断采样，不改变地形外观。

开发侧检查：相同种子采样结果一致；不同种子采样结果不同；采样与调用顺序无关；工程成功构建。

人工验证：地形仍是平地；同一次运行移动多远种子都不变化；重启后种子发生变化。

### 节点 4：连续高度图地形

状态：已通过用户验收。

改动范围：

- 用绝对世界坐标噪声生成地表高度。
- 先只生成石头、泥土和草地，不加入沙地、矿物或水。
- 调整出生高度和远裁剪面。

开发侧检查：高度合法；相邻 Chunk 边界采样连续；不同加载顺序生成结果一致；工程成功构建。

人工验证：可看到连贯的平原和缓坡；飞越至少 10 个 Chunk、穿过负坐标区域，确认没有接缝、直墙或空洞；挖掘仍正常。

### 节点 5：丘陵与低地形态调优

状态：已通过用户验收。

改动范围：

- 组合低频轮廓、细节与丘陵噪声。
- 对地貌区间进行平滑混合。
- 只调整集中配置中的频率、振幅和高度范围，不增加新方块逻辑。

开发侧检查：地表高度不越界；固定诊断种子下的抽样结果稳定；工程成功构建。

人工验证：确认地形起伏尺度、坡度与丘陵密度符合预期。此节点允许根据你的观感多轮调参，确认后才继续。

### 节点 6：沙地材质分层（无水）

状态：已通过用户验收。

改动范围：

- 按已确认方案保留“无水”设计。
- 海平面附近低地使用沙子，其他区域保持草/泥土分层。
- 不增加透明方块或水面渲染。

开发侧检查：沙子只出现在配置范围内；地下层没有意外空气；工程成功构建。

人工验证：低地可辨认出沙地，草地到沙地过渡可接受，且世界中没有假水面或透明渲染问题。

### 节点 7：煤矿生成

状态：已通过用户验收。

改动范围：

- 使用独立盐值的三维确定性采样，仅把部分石头替换成煤矿。
- 不实现洞穴和其他矿物。

开发侧检查：煤矿不出现在空气、草、泥土、沙或基岩中；结果不受加载顺序影响；工程成功构建。

人工验证：挖开地下能找到煤矿；矿量不过密；地表和 Chunk 边界没有异常。

### 节点 8：MVP 回归与性能记录

状态：功能回归已通过；后续性能与视觉问题已在 `TERRAIN_OPTIMIZATION_PLAN.md` 中完成优化并通过验收。

改动范围：原则上不增加功能，只修复前述节点暴露的问题，并记录数据生成和网格生成耗时。

开发侧检查：完成第 7 节自动测试和构建检查。

人工验证：完成第 7.2 节完整漫游、负坐标、挖掘、重载和随机种子检查。

完成标志：MVP 验收通过；后续是否异步生成、淘汰内存或扩大渲染距离，依据实测数据另立节点，不混入本轮。

### MVP 之后的独立节点（不属于本轮）

- 三维洞穴和矿脉团簇。
- 温度/湿度驱动的正式生物群系。
- 树木、植被和跨 Chunk 结构放置。
- 水方块、透明渲染与海洋。
- 垂直 Chunk，突破固定世界高度。
- 世界存档、版本号与生成器版本迁移。

## 9. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 一次把 Chunk 从 5×8×5 扩到 16×64×16 | 内存和网格构建时间陡增 | 连续数组、性能计时、分阶段提升渲染距离 |
| 噪声参数不合适 | 地形尖锐、过平或不可读 | 参数集中配置，保留固定测试种子和截图对比点 |
| 坐标溢出或负坐标处理不一致 | 远处或负轴出现接缝 | 固定宽度整数哈希，统一坐标转换并自动测试 |
| 异步任务与玩家修改竞争 | 被挖方块复原或旧网格覆盖新数据 | 异步化后引入区块版本/generation token，修改只由 World 合并 |
| 生成规则升级改变旧世界 | 存档不兼容 | 在持久化前定义 generator version；当前阶段固定默认算法版本 |
| 地表升高后相机出生在方块内 | 启动体验异常 | 根据出生点高度采样动态设置初始 Y，并预留安全距离 |

## 10. 已确认的决策

以下决策已于本计划审阅期间确认：

1. **Chunk 尺寸**：采用 `16 × 64 × 16`。
2. **水体范围**：首版不生成水，只用海平面控制低地沙层。
3. **世界种子**：每次应用启动随机生成一次，该次运行内保持固定。
4. **开发节奏**：按第 8 节逐节点实施；每个节点完成后由用户验证，确认通过后再进入下一节点。

随机种子必须在运行时可见或可复制，以便遇到特定地形问题时能够记录。MVP 不要求提供种子输入 UI；若需要复现，可在诊断或后续节点加入启动参数/配置入口。
