# SwiftCraft 地形性能与视觉清晰度优化计划

> 状态：待逐节点实施  
> 前置结论：地形生成内容已经完成，但性能质量门尚未通过。

## 1. 整体审计结论

原地形计划的功能范围已经实现：

- `16 × 64 × 16` Chunk 和一维连续存储。
- 地形生成器与 Chunk/World/网格职责解耦。
- 每次启动随机种子，同一次运行结果确定。
- 多尺度平原、低地、丘陵和山脊。
- 无水沙地分层。
- 三维煤矿团簇。
- 正负坐标、Chunk 边界、加载顺序和玩家修改回归通过。

尚未通过的是原计划第 7.2 节中的性能验收项：快速跨越 Chunk 边界时不能出现明显停顿。用户实测已经确认存在可感知卡顿，因此当前状态应定义为：

```text
地形内容 MVP：完成
功能正确性回归：完成
运行时性能验收：未通过
视觉清晰度验收：新增问题，待优化
```

## 2. 实测数据分析

用户提供的数据：

| 场景 | 数据生成 | 网格构建 | 同步总耗时 |
| --- | ---: | ---: | ---: |
| 首次加载 9 个 Chunk | 84.86 ms | 281.75 ms | 366.61 ms |
| 跨边界，典型值 | 28～40 ms | 161～213 ms | 190～253 ms |
| 单 Chunk 平均 | 约 9～13 ms | 约 27～35 ms | — |

60 FPS 每帧预算约为 16.7 ms。当前一次跨界把约 190～253 ms 的工作同步放在渲染帧中，相当于连续丢失约 11～15 帧，卡顿属于必然结果，不是主观错觉。

主要成本排序：

1. 网格构建占跨界总耗时约 80%～87%，是第一优化目标。
2. 新进入 3 个 Chunk 时实际重建 6 个网格，因为已有 Chunk 要移除先前暴露的边界面。
3. 网格生成内层为每个可见面重复创建方向数组、UV 数组、面坐标数组、索引数组和临时 `[Vertex]`，产生大量小分配。
4. 地形数据和顶点数组都在渲染线程同步生成；即使单项优化变快，只要集中执行仍可能产生尖峰。
5. `MTLBuffer` 也在同一个同步路径中创建，没有逐帧提交预算。

## 3. 视觉问题分析

当前“材质浑成一片、边缘发花”由三个因素叠加：

1. **图集 UV 落在图块边界**：64×64 图集由 4×4 个 16×16 图块组成，当前 UV 使用完整的 `[tileStart, tileEnd]`。图块边缘采样可能命中相邻材质，形成串色。
2. **远处没有正确的像素风格缩小过滤**：纹理虽然生成了 mipmap，但采样器没有 `mip_filter`。远处高频像素纹理会闪烁、摩尔纹化，看起来像“发花”。直接启用整张图集的 mipmap 又会把相邻图块颜色混合，因此需要先解决图集隔离。
3. **六个方向没有明暗层次**：当前着色器不使用面方向、法线或环境光，顶面和侧面亮度完全一致。即使几何边缘正确，相邻方块也容易在视觉上融为同一片纹理。

图集原图本身尺寸和像素网格正常，不需要重新绘制噪声图片或地形材质；优先修复采样方式和面向明暗。

## 4. 实施规则

- 一次只实施一个节点，完成后停止并由用户验收。
- 性能节点必须保留改动前后同一场景的数据，不能只凭体感判断。
- 视觉节点必须同时检查近距离、斜视角和远距离，避免修复串色却加重闪烁。
- 在异步化前先优化纯 CPU 热路径和重复重建，降低并发方案复杂度。
- 不在本阶段加入洞穴、树木、水、生物群系或其他内容功能。

## 5. 优化节点

### 优化节点 1：方块面向明暗分层

目标：先解决“方块轮廓融成一片”，且不改变纹理资源。

改动范围：

- 顶点携带面方向或预计算亮度。
- 顶面保持最亮，X/Z 两组侧面使用略有差异的亮度，底面最暗。
- 高亮选中效果继续在面向明暗之后叠加。
- 不引入动态光源、阴影或法线贴图。

人工验收：近距离和中距离能够清楚辨认单个方块、台阶和山坡转折；材质颜色不应被压得过暗。

### 优化节点 2：图集采样与远距稳定性

目标：消除图块串色、远距闪烁和“发花”。

分两步验证：

1. UV 缩进半个纹素，确保 level 0 采样始终位于当前 16×16 图块内部。
2. 将图集图块隔离为纹理数组切片，分别生成 mipmap，再使用 `nearest` 放大和受控 mip 缩小采样，避免整张图集的 mip 层互相混色。

不建议直接给当前整张图集启用 mip 过滤，因为低 mip 层已经混合相邻材质。

人工验收：

- 贴近方块观察时仍保持清晰像素边缘。
- 斜视草地、沙地和石头交界时没有相邻图块颜色渗入。
- 飞远后纹理不再明显闪烁或形成大面积杂点。

### 优化节点 3：网格生成热路径去分配

目标：先在同步模型下显著降低单 Chunk 的约 27～35 ms 网格耗时。

改动范围：

- 将六面方向、局部顶点、三角形顺序改为静态常量。
- 直接向最终顶点数组追加 6 个顶点，不再为每个面创建临时数组并 `map`。
- 预估并 `reserveCapacity`，减少顶点数组扩容复制。
- 避免每个方块重复构造 `faceCoords` 数组；改为按面查询纹理坐标。
- 分开记录“CPU 顶点生成”和“MTLBuffer 创建”，确认真实热点。

验收目标：同一机器、相似地形下，单 Chunk CPU 网格构建平均值至少降低 50%；若未达到，不进入异步节点，继续分析热点。

### 优化节点 4：邻居数据预生成，消除重复网格重建

目标：玩家跨过一个 Chunk 边界时，新建可见网格数从典型 6 个降到 3 个。

方案：

- 对渲染范围外额外维护一圈“只有方块数据、没有 GPU 网格”的 halo。
- 构建可见 Chunk 网格前，四侧邻居数据必须已经存在。
- 新 Chunk 进入可见范围时，其边界面从第一次构建就能正确剔除，不需要为了新邻居重建旧网格。
- halo 数据生成不能进入 `loadedChunks`，也不能产生额外绘制调用。

权衡：跨边界时可能预生成更多轻量数据，但可避免昂贵的旧网格重建。按当前数据，增加约 10 ms 数据工作来减少约 30 ms 甚至更多网格工作是有利交换。

验收目标：直线跨边界时典型日志应接近 `mesh 3`，而不是 `mesh 6`；边界不可出现多余外露面或裂缝。

### 优化节点 5：异步数据与 CPU 网格流水线

目标：把剩余的地形生成和顶点生成移出渲染线程。

状态模型：

```text
missing → generatingData → buildingVertices → readyForUpload → loaded
```

关键约束：

- 后台任务只处理值类型快照，不直接读写渲染线程正在修改的字典。
- 网格任务获得当前 Chunk 和必要邻居的不可变数据快照。
- 主线程只合并完成结果、创建/替换 GPU 资源。
- 每个任务携带 Chunk 版本和 generation token；过期结果不得覆盖玩家挖掘后的新数据。
- 已有网格在替代网格完成前继续显示，避免加载过程中闪空。

人工验收：持续飞行时输入和相机运动不因 CPU 生成工作停顿；快速折返不会出现旧任务覆盖新状态。

### 优化节点 6：逐帧上传预算与最终回归

目标：防止多个完成任务在同一帧集中创建 Buffer 或替换网格，再次形成尖峰。

改动范围：

- 每帧限制 GPU 上传的 Chunk 数量或毫秒预算。
- 优先处理距离玩家更近、位于移动方向前方的 Chunk。
- 增加帧耗时/Chunk 更新尖峰统计，而不仅是任务累计耗时。
- 回归挖掘、边界重建、负坐标、随机种子和快速往返。

最终性能验收建议：

- 渲染线程单帧 Chunk 工作预算不超过 4 ms。
- 稳态移动时不出现超过 33 ms 的 Chunk 更新帧。
- 连续跨越至少 20 个 Chunk，无肉眼可感知的输入停顿。
- 不以明显延迟加载、裂缝或错误边界面换取表面上的流畅。

## 6. 推荐执行顺序

严格按以下顺序逐项验收：

```text
视觉 1 面向明暗
  → 视觉 2 图集/远距采样
  → 性能 3 网格热路径
  → 性能 4 邻居 halo
  → 性能 5 异步 CPU 流水线
  → 性能 6 上传预算与最终回归
```

先完成两个视觉节点，便于快速确认用户反馈是否被准确解决；随后先降低工作总量，再异步化，避免把当前低效算法原样搬到后台。
